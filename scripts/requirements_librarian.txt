# Librarian live dual-model extraction — vendor SDK dependencies (L1 / D33).
# Install into the project venv:  ./.venv/bin/pip install -r scripts/requirements_librarian.txt
#
# These are needed ONLY to run the live pipeline (scripts/run_librarian.py).
# The offline pipeline + unit tests (FakeModelClient) need none of them — the
# RealModelClient imports each SDK lazily, so absence never breaks collection.
#
# Phase D (dev pair): Gemini 3.1-flash-lite + a Mistral SKU (the model id lives in
# docs/thresholds.yaml; free-tier coverage of that SKU is account-dependent).
google-genai>=1.0.0        # Gemini — client.models.generate_content, JSON output
mistralai>=1.0.0           # Mistral — client.chat.complete, JSON output (v1 + v2 SDKs supported)

# Phase F (reported figures, gated on SKU/cost authorization, D33): Claude Sonnet 4.6 + Gemini 3.5-flash.
# Enabled for phase_f. The live run is gated on ANTHROPIC_API_KEY + Gemini
# quota + the D33 authorization; installing the SDK runs nothing on its own.
anthropic>=0.40.0
