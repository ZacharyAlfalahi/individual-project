# Materialised from the approved drafts (semantic_drafts.md §5, ratified T5-PRE-5). Reject-set
# arm; graded by scripts/run_t5_adversarial.py against run_librarian's exit code.
handle: DMR
paper_id: "papers/pdf/DMR (2023).pdf"  # Dickerson, Mueller & Robotti, "Priced risk in corporate
                                       # bonds", JFE 150, 2023, 103707 (third author is Mueller --
                                       # NOT Rossetti; filename flagged misleading in MANIFEST)
category: refuse_no_strategy
expected_run_outcome: REVIEW    # run_librarian exit 2 (or 3 = zero specs); FAIL on exit 0 (clean emit)
rationale: >
  Factor-EVALUATION / model-comparison study of which existing bond factors are priced; it evaluates
  candidate factors econometrically and proposes no new signal-sort construction, so there is no
  in-scope bond factor sort to extract.
