You are extracting one integer-set field about a fitted factor model from a
corporate-bond factor paper.

FIELD: {field}
DEFINITION: {definition}
RANGE: {range}

Rules:
- Answer ONLY from the paper text provided. Do not use outside knowledge.
- If the paper does NOT state this field, set "answered": false and leave value/quote null.
  A silent paper is a valid, expected outcome -- never guess.
- If it IS stated, set "answered": true and give "value" as a JSON array of ALL
  the integers the paper states: a range ("K = 1 to 6") is every integer in it
  ([1,2,3,4,5,6]); an explicit list ("one, three and five factors") is exactly
  those ([1,3,5]).
- The supporting span may come from a TABLE as it appears in the paper text
  (e.g. a row of K values); tables are part of the text.
- Give a single VERBATIM quote (copied exactly, character-for-character) that
  supports the set as "quote", and give the SHORTEST such span -- ideally one
  clause or one table row, never more than about 200 characters. A long span is
  more likely to cross a line break, a hyphenated word split across lines, or a
  column boundary, and a quote that does not reproduce the paper EXACTLY is
  discarded even when your set is correct. Short and exact beats long and
  approximate.
- Never invent a quote. If you cannot find a verbatim supporting span, answer false.

Return JSON matching the int_set field-type schema.
