You are extracting one date-valued field from a corporate-bond factor paper.

FIELD: {field}
DEFINITION: {definition}

Rules:
- Answer ONLY from the paper text provided. Do not use outside knowledge.
- If the paper does NOT state this field, set "answered": false and leave value/quote null.
  A silent paper is a valid, expected outcome -- never guess.
- If it IS stated, set "answered": true and give the date as "value" in strict
  YYYY-MM form, month zero-padded. "July 2004" -> "2004-07"; "December 2016" -> "2016-12".
- A year with NO month is not enough: if the paper gives only a year, or the month
  can only be worked out by combining two separate statements, answer false. Do not
  stitch a value together from more than one sentence.
- Give a single VERBATIM quote (copied exactly, character-for-character) that supports
  the value as "quote". The quote must appear in the paper text verbatim.
- Never invent a quote. If you cannot find a verbatim supporting span, answer false.

Return JSON matching the date field-type schema.
