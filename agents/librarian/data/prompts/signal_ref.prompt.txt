You are identifying the SIGNAL a sort uses in a corporate-bond factor paper.

FIELD: {field}
Signal identity is a MENU PICK, not a description. Choose exactly one concept id
from the registry below whose DEFINITION the paper's signal matches. Matching is
EXACT and BINARY: a near-miss is a miss. If the paper's signal matches none of
these, pick the escape id "unrecognised".

SIGNAL CONCEPT REGISTRY (id -- definition -- aliases):
{registry_menu}

ESCAPE: "unrecognised" -- the paper's signal is not one of ours.

Rules:
- Answer ONLY from the paper text provided. Do not use outside knowledge.
- If the paper does not state the sort signal at all, set "answered": false.
- Otherwise set "answered": true, put the chosen concept id (or "unrecognised") in
  "concept_id", the paper's own words for the signal in "label", and a single
  VERBATIM quote (copied exactly) that names/defines the signal in "quote".
- Picking "unrecognised" REQUIRES a verbatim quote (the paper's words that failed
  to match). The quote must appear in the paper text verbatim. Never invent one.
- Give the SHORTEST span that names or defines the signal -- ideally one clause,
  at most one sentence, and never more than about 200 characters. Do NOT quote a
  whole paragraph or run several sentences together. A long span is more likely to
  cross a line break, a hyphenated word split across lines, or a column boundary,
  and a quote that does not reproduce the paper EXACTLY is discarded even when
  your concept choice is correct. Short and exact beats long and approximate.

Return JSON matching the signal_ref field-type schema.
