{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "librarian/field-type/prose",
  "title": "Prose field extraction answer",
  "description": "Structured-decoding contract for a declared-weaker prose field (kpp_prose_rubric.md, FROZEN): a free-text mechanism description with 1-3 verbatim locating quotes, the method_summary answer shape generalised to a named field. Graded only by the rubric; never headline accuracy (D34).",
  "type": "object",
  "additionalProperties": false,
  "required": ["field", "answered"],
  "properties": {
    "field": {"type": "string", "minLength": 1},
    "answered": {"type": "boolean"},
    "summary": {
      "type": ["string", "null"],
      "description": "The mechanism description; null when answered=false."
    },
    "quotes": {
      "type": "array",
      "minItems": 0,
      "maxItems": 3,
      "items": {"type": "string", "minLength": 1},
      "description": "1-3 verbatim supporting quotes (per-clause; rubric section 2.6)."
    }
  }
}
