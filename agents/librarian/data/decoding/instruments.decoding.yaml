# Decoding config for run-template: instruments (Scope B, 2026-09-04).
#
# Temperature 0 + structured (JSON-schema-constrained) decoding, per D10/D33.
# k=1 is the primary sampling protocol (D10); the k=3 / 2-of-3 ladder is
# pre-registered but NOT built (trigger = gold STATED coverage < 80%).
# Whole-paper structured call (~29 rows with quotes) -- the larger cap.
run_template: instruments
temperature: 0
top_p: 1
structured: true
schema: schemas/instruments.schema.json
k: 1
max_output_tokens: 4096
