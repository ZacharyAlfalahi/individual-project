"""
shared — cross-agent modules that live OUTSIDE any single agent package.

`shared/handoff/` holds the R2 seam (the only place an audit magnitude becomes a verdict).
`shared/stats/` and `shared/evaluation/` (later) will host the CPCV / crowding /
cost machinery called by the Experimentalist.
"""
