# Licensed / raw data — NEVER commit (FISD + TRACE). Enforced by .githooks + CI;
# see docs/data_governance.md and scripts/check_no_licensed_data.sh.
data/fisd/
data/**/*.parquet
data/**/*.csv
data/**/*.csv.gz
data/**/*.json
data/trace_enhanced_*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Coverage artefacts (test-time only)
.coverage
.coverage.*
htmlcov/
coverage.xml

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store


# Local workflow notes — not synced to remote
tasks/

