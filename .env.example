# Librarian live extraction — API keys (D33 two-phase model stack).
# Copy to .env (git-ignored) and fill in. NEVER commit real keys.
#   cp .env.example .env   &&   $EDITOR .env
# The RealModelClient reads each key from the env var named in
# docs/thresholds.yaml -> librarian.model_stack.*.api_key_env.

# Phase D — free development pair (non-reportable)
GEMINI_API_KEY=            # Google AI Studio: https://aistudio.google.com/apikey
MISTRAL_API_KEY=           # Mistral console: https://console.mistral.ai/

# Phase F — reported figures (gated on SKU authorization); uncomment when wiring
# ANTHROPIC_API_KEY=       # https://console.anthropic.com/
